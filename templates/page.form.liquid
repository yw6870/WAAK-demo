<div class="page-width">
  <div class="grid">
    <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">

      {%- assign formId = 'ContactForm' -%}
      {% form 'contact', id: formId %}
        {% comment %} #####################        送信が成功したときの処理 {% endcomment %}
        {% if form.posted_successfully? %}

          <div class="section-header text-center">
            <h1>THANK YOU</h1>
            <p>お問い合せありがとうございます</p>
          </div>
        {% comment %} #####################        送信が成功したときの処理 {% endcomment %}

        {% else %}

        {% comment %} #####################        送信が失敗したときの処理 {% endcomment %}

          <div class="section-header text-center">
            <h1>CONTACT</h1>
            <p>お問い合わせ</p>
          </div>

          <div class="rte">
            <p>WAAKに関するお問い合わせや採用ご希望など、すべてのお問い合わせはこちらのフォームからお受けしておりますフォーム送信後は担当者より折り返しご連絡しますので少々お待ちください。</p>
          </div>

          <div class="contact-form form-vertical">
            {{ form.errors | default_errors }}

            {% include 'form-status', form: form, form_id: formId %}
            <div class="form">
              <label for="ContactForm-contac-type">お問い合わせ種別</label>
              <select id="ContactForm-contac-type" name="contact[お問い合わせ種別]">
                <option>サンプル１</option>
                <option>サンプル２</option>
                <option>サンプル３</option>
              </select>
            </div>

            <div class="form">
              <label for="{{ formId }}-name">{{ 'contact.form.name' | t }}</label>
              <input required class="required" type="text" id="{{ formId }}-name" name="contact[{{ 'contact.form.name' | t }}]" value="{% if form[name] %}{{ form[name] }}{% elsif customer %}{{ customer.name }}{% endif %}" placeholder="仕事　太郎">
            </div>

            <div class="form">
              <label for="{{ formId }}-name-furigana">フリガナ</label>
              <input type="text" id="{{ formId }}-name-furigana" name="contact[フリガナ]" value="" placeholder="シゴト　タロウ">
            </div>

            <div class="form">
              <label for="{{ formId }}-company-name">会社名</label>
              <input type="text" id="{{ formId }}-company-name" name="contact[会社名]" value="" placeholder="ワアク株式会社">
            </div>

            <div class="form">
              <label for="{{ formId }}-email">メールアドレス<span aria-hidden="true">*</span></label>
              <input
                type="email"
                id="{{ formId }}-email"
                name="contact[email]"
                autocorrect="off"
                autocapitalize="off"
                value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
                aria-required="true"
                placeholder="sample@waak.space"
                {%- if form.errors contains 'email' -%}
                  class="input--error"
                  aria-invalid="true"
                  aria-describedby="{{ formId }}-email-error"
                {%- endif -%}
                >
              {%- if form.errors contains 'email' -%}
                <span id="{{ formId}}-email-error" class="input-error-message">
                  <span class="visually-hidden">{{ 'general.accessibility.error' | t }} </span>
                  {% include 'icon-error' %}
                  <span>{{ form.errors.translated_fields['email'] | capitalize }} {{ form.errors.messages['email'] }}.</span>
                </span>
              {%- endif -%}
            </div>

            <div class="form">
              <label for="{{ formId }}-phone">{{ 'contact.form.phone' | t }}</label>
              <input type="tel" id="{{ formId }}-phone" name="contact[{{ 'contact.form.phone' | t }}]" pattern="[0-9\-]*" value="{% if form[phone] %}{{ form[phone] }}{% elsif customer %}{{ customer.phone }}{% endif %}" placeholder="080********">
            </div>

            <div class="form">
              <label class="textarea-label" for="{{ formId }}-message">お問合せ内容</label>
              <textarea class="textarea" rows="10" id="{{ formId }}-message" name="contact[{{ 'contact.form.message' | t }}]" placeholder="お問合せ内容をご入力ください。">{% if form.body %}{{ form.body }}{% endif %}</textarea>
            </div>

            <div class="checkbox">
              <label for="comfirm-checkbox-btn">お問合せ内容</label>
              <input  type="checkbox" id="comfirm-checkbox-btn" area="hidden">
            </div>

            <div class="form-p">
              <p>ご入力いただいた個人情報は、お問い合わせへの回答・連絡のみに利用いたします。</p>
              <p>ご入力いただいた個人情報に関しては、<a href="#" style="border-bottom: 1px solid black;line-height: 2em;">プライバシーポリシー</a>をご参照ください。</p>
            </div>
          </div>

        <input type="submit" class="comfirm-btn" value="入力内容を確認する">

        {% endif %}
        {% comment %} #####################        送信が失敗したときの処理 {% endcomment %}
      {% endform %}
    </div>
  </div>
</div>

<style>
  @media screen and (min-width: 750px) {
    .contact-form {
      display: flex;
      flex-direction: column;
    }
    .form {
      display: flex;
      justify-content: space-between;
      align-items: center !important;
      padding : 25px 0;
      border-top: 1px solid rgb(220, 220, 220);
    }
    .form:last-child {
      border-bottom: 1px solid rgb(220, 220, 220);
      margin-bottom: 50px;
    }
    label {
      width: 150px;
      margin: 0 !important;
      padding: 10 0;
    }
    .textarea-label {
      line-height: 2em;
      display: block !important;
      align-self: flex-start !important;
    }
    input,textarea,select {
      width: calc(95% - 150px) !important;
      margin: 0 !important;
      padding: 0 50px;;
      line-height: 4em;
      border: none;
      {% comment %} background: rgb(255, 245, 245); {% endcomment %}
    }
    .textarea {
      line-height: 2em;
    }
    ::placeholder {
      color: rgb(160, 160, 160);
    }
    .checkbox label {
      display: block;
      line-height: 2.5em;
      text-align: center;
      margin: 0 auto !important;
      width: 300px !important;
      border: 1px solid black;
    }
    .checkbox label:hover {
      transition: 0.5s;
      background: black;
      color: white;
    }
    .checkbox label:checked {
      display: none;
    }
    .form-p {
      padding-top: 50px;
    }
    .form-p p {
      margin-bottom: 0;
      line-height: 2em;
      text-align: center;
    }
  }
  @media screen and (max-width: 749px) {
    
  }
</style>